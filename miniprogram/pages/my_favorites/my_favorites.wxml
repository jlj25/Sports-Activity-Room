<view class="my-favorites-container">
  <view wx:if="{{favorites.length === 0}}" class="empty">暂无收藏</view>
  <block wx:for="{{favorites}}" wx:key="id">
    <view class="favorite-item" bindtap="goToDetail" data-id="{{item.id}}">
      <image class="cover" src="{{item.cover}}" mode="aspectFill" />
      <view class="info">
        <view class="title">{{item.name}}</view>
        <view class="time">时间：{{item.business_hours}}</view>
        <view class="location">地点：{{item.location}}</view>
      </view>
    </view>
  </block>
</view>

